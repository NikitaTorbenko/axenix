import styles from "./Info.module.scss";
import { Item } from "./Item";

export const Info = () => {
  return (
    <div className={styles.root}>
      {/* {fetchData.map((el) => (
        <Item {...el} />
      ))} */}
    </div>
  );
};
