import styles from "./History.module.scss";

export const History = () => {
  return (
    <div className={styles.root}>
      {/* {fetchData.map((el) => (
        <Item {...el} />
      ))} */}
    </div>
  );
};
